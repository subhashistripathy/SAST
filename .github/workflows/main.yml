name: Snyk Code Scan (SAST + SARIF)

on:
  push:
    branches:
      - '**'  # all branches
  pull_request:
    branches:
      - '**'  # all branches
  workflow_dispatch:

jobs:
  snyk-code-scan:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      security-events: write

    steps:
      # Checkout the repo
      - name: Checkout repository
        uses: actions/checkout@v4

      # Setup Node.js (required for Snyk CLI)
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      # Install Snyk CLI
      - name: Install Snyk CLI
        run: npm install -g snyk

      # Authenticate Snyk using secret token
      - name: Authenticate Snyk
        env:
          SNYK_TOKEN: c80a2012-bb87-4482-beb5-399c664f183d
        run: snyk auth "$SNYK_TOKEN"

      # Run Snyk Code scan and generate SARIF
      - name: Run Snyk Code Test (Generate SARIF)
        env:
          SNYK_TOKEN: c80a2012-bb87-4482-beb5-399c664f183d
        run: |
          snyk code test \
            --sarif-file-output=snyk-code.sarif \
            --severity-threshold=low || true
          
          # Verify SARIF exists
          ls -lh snyk-code.sarif

      # Upload SARIF to GitHub Code Scanning
      - name: Upload SARIF to GitHub
        uses: github/codeql-action/upload-sarif@v4
        with:
          sarif_file: snyk-code.sarif
